{{ mode }}
----------------------------------------------------------------------------------------------------------------
KHz      ID         Type         Logs Latest       PWR GSQ    SP ITU Location
----------------------------------------------------------------------------------------------------------------
{% for s in signals %}
{{
    "% -8s"  | format((0 + s.khz)) }} {{
    "% -10s" | format(s.call) }} {{
    "% -10s" | format( typeRepository.typeForCode(s.type)['label']|trans) }} {{
    "% 6s"   | format(s.logs) }} {{
    "% 10s"  | format(s.last_heard|date('Y-m-d')) }} {{
    "% 5s"   | format(s.pwr ? s.pwr : '') }} {{
    "% 6s"   | format(s.GSQ) }} {{
    "% 2s"   | format(s.SP) }} {{
    "% 3s"   | format(s.ITU) }} {{
    s.QTH|raw
}}
{% endfor %}