{% extends 'base.html.twig' %}

{% block title %}{{ system|upper }} &gt; Listeners List{% endblock %}

{% block body %}
{% include 'menu/menu.html.twig' %}

<div class="main">
    <h1>{{ mode }}</h1>
    {{ text|raw }}
    {% set form_title='Customise ' ~ system|upper ~ ' Listener Report' %}
    {% include 'forms/listener/search.html.twig' %}
</div>
{% include 'grids/listener/results.html.twig' %}

<script>
    $(document).ready( function() {
        setFormTypesStyles();
        setFormTypesDefault();
        setFormTypesAllAction();
        setColumnSortActions();
        setColumnSortedClass();
        setExternalLinks();
        $('#form_filter_help').html('{{ matched }}');
    });
</script>
{% endblock %}
