{% if system == "reu" %}
            body { background: #fff8f8; }
            h1 { color: #ff3333; }
{% endif %}
{% if system == "rna" %}
            body { background: #f8f8ff; }
            h1 { color: #3333ff; }
{% endif %}
{% if system == "rww" %}
            body { background: #fafffa; }
            h1 { color: #33a033; }
{% endif %}
