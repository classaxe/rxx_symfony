{% extends 'main.html.twig' %}

{% block body %}
    <div class="main">
        {% include 'users/list.html.twig' %}
    </div>
    <script>
        initUsersForm(msg.paging_u, {{ results.total }});
    </script>
{% endblock %}
