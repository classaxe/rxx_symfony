{{ title }}
Output sorted by Date
---------------------------------------------------------------------------------------------------
YYYYMMDD UTC  KHz      ID            KM    Mi   PWR GSQ    SP ITU Location
---------------------------------------------------------------------------------------------------
{% for log in logs %}
{{
    log.date | date('Ymd') }} {{
    "% 4s"   | format(log.time) }} {{
    "% -8s"  | format((0 + log.khz)) }} {{
    "% -10s"  | format(log.call) }} {{
    "% 5s"   | format(log.dxKm) }} {{
    "% 5s"   | format(log.dxMiles) }} {{
    "% 5s"   | format(log.pwr ? log.pwr : '') }} {{
    "% 6s"   | format(log.gsq) }} {{
    "% 2s"   | format(log.sp) }} {{
    "% 3s"   | format(log.itu) }} {{
    log.qth|raw
}}
{% endfor %}