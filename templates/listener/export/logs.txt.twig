{{ title }}
{{ subtitle }}
---------------------------------------------------------------------------------------------------
YYYYMMDD UTC  KHz      ID         Type           KM  Miles   PWR GSQ    SP ITU Location
---------------------------------------------------------------------------------------------------
{% for l in logs %}
{{
    l.date | date('Ymd') }} {{
    "% 4s"   | format(l.time) }} {{
    "% -8s"  | format((0 + l.khz)) }} {{
    "% -10s" | format(l.call) }} {{
    "% -10s" | format( typeRepository.typeForCode(l.type)['label']|trans) }} {{
    "% 6s"   | format(l.dxKm) }} {{
    "% 6s"   | format(l.dxMiles) }} {{
    "% 5s"   | format(l.pwr ? l.pwr : '') }} {{
    "% 6s"   | format(l.gsq) }} {{
    "% 2s"   | format(l.sp) }} {{
    "% 3s"   | format(l.itu) }} {{
    l.qth|raw
}}
{% endfor %}