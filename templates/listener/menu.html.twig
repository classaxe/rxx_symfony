<div id="tabsPanel">
{% for menuOption in menuOptions %}
    <a class="button small{% if app.request.get('_route') == menuOption[0] %} is-active{% endif %}"
       href="{{ path(menuOption[0], { 'system' : system, 'id' : id}) }}">{{ menuOption[1] }}</a>
{% endfor %}
</div>