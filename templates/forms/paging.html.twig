{% if form.limit is defined %}
    {{ form_row(form.limit) }}
    {{ form_row(form.prev) }}
    {{ form_row(form.next) }}
    {{ form_row(form.page) }}
    <div id="form_paging_status"></div>
{% endif %}

<script>
var paging = {
    limit: {{ results.limit  }},
    page: {{ results.page  }},
    total: {{ results.total }}
}
</script>