{% if lastError %}
    <div id="lastError"><strong>Error:</strong> {{ lastError }}</div>
{% endif %}
{% if lastMessage %}
    <div id="lastMessage"><strong>Message:</strong> {{ lastMessage }}</div>
{% endif %}